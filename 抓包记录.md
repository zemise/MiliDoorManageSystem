# 抓包记录：门禁网段 门禁/智能面板

记录日期：2026-05-31

## 当前接线

现场使用抓包器进行旁路监听：

```text
电脑网线 -> 抓包器监听口
智能面板原网线 -> 抓包器通信口1
抓包器主干口2 -> 智能面板
```

注意：监听口通常只能抓包，不一定能作为网络里的正常主机主动发包。因此当前接法适合分析协议，但不适合直接运行控制脚本去开锁。

## 已知 IP

抓包中出现：

```text
面板IP
门禁IP
```

当前电脑曾测试 `ping 面板IP`，结果为 100% 丢包。电脑当时主要网卡 IP 为 `家庭WiFiIP/24`，与 `门禁网段` 不在同一网段，因此此结果不能证明设备离线，只说明当前电脑网络路径不可达或监听口不能发包。

## 已确认开锁相关报文

Wireshark 显示过滤器：

```wireshark
udp.port == 14301 && ip.addr == 面板IP && frame contains aa:f0
```

点开门后抓到以下关键记录：

```text
No: 619
Time: 1350.770059
Source: 面板IP
Destination: 门禁IP
Protocol: UDP
Length: 114 bytes on wire
UDP: 14301 -> 14301
UDP payload Len: 72
```

UDP payload：

```text
08ff0108000000000000000000000000534700001f0200002b000000000028001f02000001010100320001000000000046b4e41131000100000000000000000032aaf0a000010000
```

关键特征：

```text
08 ff 01 08            协议头
53 47 00 00            "SG\0\0"
aa f0 a0 00 01 00 00   开锁动作标记
```

## 初步判断

这条开锁报文方向是：

```text
面板IP -> 门禁IP
```

因此在这次抓包里：

- `面板IP` 更像是发出控制命令的一端，可能是智能面板/主控。
- `门禁IP` 更像是接收开锁命令的一端，优先作为门禁控制目标测试。

后续不要先把 `面板IP` 当成 `DOOR_IP_1`。应优先验证 `门禁IP`。

## 项目状态

`protocol.py` 中的默认空闲态开锁报文已替换为**现场抓包 payload**（2026-09-17 起）：

```text
08ff0108000000000000000000000000534700001f0200002b000000000028001f02000001010100320001000000000046b4e41131000100000000000000000032aaf0a000010000
```

它同样是 72 字节 UDP payload，并包含 `aa f0 a0`，已在 `门禁IP` 实测开门成功。

已经修改 `unlock_test.py`，新增参数：

```bash
--payload-hex
```

用途：直接重放抓包得到的 UDP payload，便于先验证现场设备是否接受该报文。

语法检查已通过：

```bash
python3 -m py_compile unlock_test.py protocol.py
```

## 后续测试前提

要主动发开锁命令，电脑需要作为正常主机接入 `门禁网段` 网络，而不是只接抓包器监听口。

### Mac 当前实测状态

2026-05-31 实测 Mac 当前有线网卡：

```text
接口: en6
MAC: 04:7c:16:1b:27:b7
状态: inactive
IPv4: 无
```

当时 Wi-Fi 网卡：

```text
接口: en0
IPv4: 家庭WiFiIP/24
默认路由: 家庭网关IP
```

结论：Mac 当前还没有通过有线网卡接入门禁链路。此时运行开锁脚本，数据不会正确进入 `门禁网段` 门禁网络。

2026-06-03 再次复测：

```text
接口: en6
MAC: 04:7c:16:1b:27:b7
状态: inactive
IPv4: 无
```

当时 Wi-Fi 网卡：

```text
接口: en0
IPv4: 家庭WiFiIP/24
默认路由: 家庭网关IP
```

结论不变：当前 Mac 有线口仍未建立链路，不能向 `门禁IP` 或 `面板IP` 所在网络发控制包。

推荐方式：

```text
电脑普通网卡 -> 现场交换机/路由器普通口
电脑 IP: 调试主机IP
掩码: 255.255.255.0
网关: 可留空，或填现场网关
```

也可以使用双网卡：

```text
网卡1 -> 抓包器监听口，用于 Wireshark 抓包
网卡2 -> 现场真实网络，用于发送 UDP 控制包
```

## 下一步验证命令

在电脑确认接入 `门禁网段` 网络后，优先重放现场抓到的开锁 payload 到 `门禁IP`：

```bash
python3 unlock_test.py 门禁IP --skip-ping --print-hex --payload-hex 08ff0108000000000000000000000000534700001f0200002b000000000028001f02000001010100320001000000000046b4e41131000100000000000000000032aaf0a000010000
```

如果成功开门，说明：

```bash
export DOOR_IP_1="门禁IP"
```

更可能是正确配置。

如果未成功，继续抓包观察是否有响应，重点确认：

- 发包网卡是否真的在 `门禁网段/24`
- 发出的 UDP 源/目标是否为 `14301 -> 14301`
- 目标是否应为 `门禁IP`
- 设备是否校验源 IP 或源 MAC
- 设备是否要求源端口固定为 `14301`

## Wireshark 常用过滤器

只看控制端口：

```wireshark
udp.port == 14301
```

只看 `.49` 相关控制包：

```wireshark
udp.port == 14301 && ip.addr == 面板IP
```

查找包含协议命令标记的包：

```wireshark
udp.port == 14301 && frame contains aa:f0
```

查找开锁动作：

```wireshark
udp.port == 14301 && frame contains aa:f0:a0
```

后续如果点视频/通话，可能还会看到大量视频/音频 UDP/RTP 流量。开锁分析优先关注 `14301/UDP`，不要被视频流量干扰。

## 2026-09-17 实测结论（LicheeRV Nano 常驻网关）

- 板子接入门禁网络：`eth0 = 调试主机IP/24`（无网关）。
- `ping 门禁IP` 通（0.7~2ms）；`ping 面板IP` 不通 —— **确认 `.21` 是门禁控制目标**。
- 重放本记录中的开锁 payload 到 `.21:14301`，**门禁返回 72 字节 ACK**（响应帧 kind=0x17，含 `f0 a0` 标记），**实门打开**。
- MQTT 链路：板载 amqtt broker（`127.0.0.1:1883`）→ `gateway.py`（MQTT rc=0）→ UDP 14301 转发，源端口 14301，端到端验证通过。
- 板子重启后 WiFi 静态 IP、eth0 静态 IP、broker、gateway 均自动恢复（`/etc/init.d/S99mili`）。
- 遗留：手机/APP 走 WiFi 直连板子 broker 可能受 2.4G/5G 跨频段互访限制，需要关闭主路由/AP 的"AP 隔离/用户隔离"，或把 broker 迁到有线设备。
